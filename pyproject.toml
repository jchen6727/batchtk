[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "batchtk"
version = "0.0.1a"
authors = [
    { name = "James Chen", email = "jchen.6727@gmail.com" },
]
description = "Batch submission toolkit for Python"
dependencies = ['importlib-metadata; python_version>"3"']
readme = "README.md"
